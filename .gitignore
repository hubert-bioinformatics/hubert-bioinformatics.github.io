# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
scripts/.gist-cache/
scripts/.migration-report.json
# Radar 가 매 회차 만드는 PR 본문. 저장소에 남길 이유가 없다.
radar-pr-body.md
# 노트북 변환 전 원본. 되돌릴 때만 쓰고 커밋하지 않는다 (glob 로더가 .md 만 읽지만
# 실수로 들어가면 235개 글 옆에 중복이 쌓인다)
*.ipynb-backup
photos-inbox/*
!photos-inbox/.gitkeep
